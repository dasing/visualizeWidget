# Execution
```
  python visualize.py INPUT_TXT_PATH INPUT_IMAGE_PATH OUT_DIR [OPTIONS]
  ex: python visualize.py output/001.txt MMAI_YOLO_testData/001.jpg outputImage

  OPTIONS
  --draw_box: draw bounding box(1) or not(0), default is 1
  --draw_center: draw center(1) or not(0), default is 1
```

